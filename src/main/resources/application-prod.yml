server:
  port: 8443
  ssl:
    enabled: true
    key-store:
      classpath: keystore.jks
    key-store-password: ${KEY-STORE-PASSWORD}
    key-password: ${KEY-PASSWORD}
    keyStoreType: PKCS12
    keyAlias: springkey
    security:

spring:
  profiles: prod
